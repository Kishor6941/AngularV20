import { PracDirective } from './prac-directive';

describe('PracDirective', () => {
  it('should create an instance', () => {
    const directive = new PracDirective();
    expect(directive).toBeTruthy();
  });
});
